version: '3'
services:
  postgres:
    image: postgres:latest
    ports:
        - "5433:5433"
  app:
      build:
        context: ..
        dockerfile: ./build/package/Dockerfile
      command: ["make", "run"]
      ports:
        - "3009:3009"